using UnityEngine;

public class DoorButtonReference : MonoBehaviour
{
    // Drag the main door object (the one with DoorInteraction.cs) into this slot in the Inspector
    public DoorInteraction targetDoor; 
}